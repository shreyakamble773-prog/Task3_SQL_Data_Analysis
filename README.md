 Task 3: SQL for Data Analysis

Dataset:olist_customers_dataset (CSV)

Objective:Use SQL queries to extract and analyze data.

Queries included:
1. Basic SELECT queries  
2. WHERE filters  
3. ORDER BY  
4. GROUP BY  
5. Subqueries  
6. Aggregate functions: COUNT, COUNT DISTINCT  
7. Self-join  
8. Views  

*\Instructions:
- All queries can be run in SQLite (DB Browser).  
- Screenshots of outputs are included in the PDF.
